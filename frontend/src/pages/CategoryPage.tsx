import React, { useState } from "react";
import Header from "../components/Header";
import Footer from "../components/Footer";
import { categoryEnum } from "../types/ProductInterface";

const CategoryPage: React.FC = () => {
  // const [Filter, setFilter] = useState<categoryEnum>(categoryEnum.bracelets);

  // const filterFn = (value: categoryEnum) => {
  //   setFilter(categoryEnum.bracelets);
  // };
  return <></>;
};

export default CategoryPage;
